# nx-container

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-container` to build the library.

## Running unit tests

Run `nx test nx-container` to execute the unit tests via [Vitest](https://vitest.dev/).
