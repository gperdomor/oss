name: Publish Package

on:
  push:
    tags:
      - 'v*'
      - '*.*.*'
      - '*.*.*-*'

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  publish:
    # prevents this action from running on forks
    if: github.repository == 'gperdomor/oss'
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          filter: tree:0
          fetch-depth: 0 # include tags

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
          package-manager-cache: false # never use caching in release builds

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Print Environment Info
        run: pnpm exec nx report

      - name: Building packages
        run: pnpm exec nx run-many --target=build --no-cloud
      - name: Determine npm tag
        id: npm-tag
        run: |
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          if [[ "$TAG_NAME" =~ -(alpha|beta|rc|next|canary) ]]; then
            echo "tag=next" >> $GITHUB_OUTPUT
            echo "Pre-release detected. Using 'next' tag."
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
            echo "Stable release detected. Using 'latest' tag."
          fi

      - name: Publish packages
        run: pnpm exec nx release publish --access=public --tag=${{ steps.npm-tag.outputs.tag }} --no-cloud
        env:
          NPM_CONFIG_PROVENANCE: true
